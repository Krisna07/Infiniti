/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
};
module.exports = {
  images: {
    domains: ["i.seadn.io"],
  },
};

module.exports = nextConfig;
